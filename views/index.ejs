<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Book Collection</title>
    <link href="/styles/reset.css" rel="stylesheet" type="text/css" />
    <link href="/styles/base.css" rel="stylesheet" type="text/css" />

  </head>
  <body>
    <main class=collections>
      <h1>My Book Collection:<%=' ' + bookNumber + " Total books in Library" %></h1>
      <% bookArray.forEach(element => { %>
        <a class="button" href="./searches/">New Search</a>
      <section class="book-view-container">
        <ul>
          <li class="title"><%='Title: ' + element.title + '.'%></li>
          <li class="author"><%='Author: ' + element.author + '.'%></li>
          <li class= "isbn"><%='ISBN: ' + element.isbn + '.'%></li>
          <li class= "description"><%='Description: ' + element.description + '.'%></li>
          <li class= "bookshelf"><%='Bookshelf: ' + element.bookshelf + '.'%></li>
          <li><img src='<%= element.image_url %>'></img></li>
        </ul>
      </section>
      <% }) %>
    </main>
  </body>
</html>